import { Options, Vue } from "vue-class-component";
import IconCenter       from "@/components/Atoms/IconCenter/index.vue";


@Options( {
    components: {
        IconCenter
    }
})
export default class StaticListWithText extends Vue {
}